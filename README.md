
# Viht-Game Setup Instructions

## 🚨 ГЛАВНАЯ ОШИБКА: ГДЕ ИСКАТЬ URL MAPPINGS
Ты смотрел в разделе **Discord Social SDK**, а нужно смотреть НИЖЕ, в раздел **Activities**.
Посмотри на свой скриншот меню слева:

1. Сначала идет раздел `Discord Social SDK` (Overview, Downloads...). **Это не то**.
2. Ниже идет раздел `Activities` (Settings, **URL Mappings**...). **ТЕБЕ НУЖНО СЮДА!**

### ✅ ИНСТРУКЦИЯ ПО ШАГАМ:
1. В меню слева найди заголовок **Activities** (он под Social SDK).
2. Нажми на **URL Mappings**.
3. Нажми кнопку **Add New Mapping**.
4. В поле **Prefix** напиши: `/`
5. В поле **Target** вставь ссылку на сайт: `https://helpful-manatee-cf042c.netlify.app`
6. Нажми **Save** (или Finish).

*Без этого шага кнопка "Запустить" не будет открывать игру!*

---

## 🤖 БОТ ДЛЯ КАНАЛА (АВТО-ПОСТИНГ)

Чтобы бот отправлял красивую кнопку **PLAY**:

1. **Вставь токен:** 
   Открой файл `supabase/functions/discord-publisher/index.ts` и вставь токен бота.
   
2. **Загрузи функцию:**
   ```bash
   npm run deploy:discord
   ```

3. **Включи таймер (Cron):**
   В Supabase -> SQL Editor выполните:
   ```sql
   select cron.schedule('discord-auto-post', '0 */6 * * *', $$
     select net.http_post(
         url:='https://irtwyprryptdqtusxjvc.supabase.co/functions/v1/discord-publisher',
         headers:='{"Content-Type": "application/json"}'::jsonb,
         body:='{}'::jsonb
     ) as request_id;
   $$);
   ```

Теперь бот будет обновлять сообщение каждые 6 часов.
